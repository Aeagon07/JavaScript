"use strict"; // treat all JS code as newer version

// alert( 3 + 3) we are using the node, not the browser

let name = "Rushikesh"
let age = 20
let isLoggedIn = true 

/*
number => 2 to power 53
bigint 
string => " "
boolean => true/false
null => standalone value (Isse hame assign bhi karte hai)
undefined => when you not given the value to variable 
symbol => used to identify the uniqueness in the code (use case in React)
these all are the primative data type 
*/

console.log(typeof age);

// ****** Interview Question *********
// Undefined is type if you write typeof undefined then it print you the undefined but When you write the null it gaves you type Object.

console.log(typeof undefined); // Undefined
console.log(typeof null); // Object

